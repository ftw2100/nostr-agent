# Nostr Configuration
# Your Nostr private key in nsec format (get one from a Nostr client like Damus, Amethyst, etc.)
NOSTR_NSEC=nsec1your_private_key_here_replace_with_your_actual_nsec_key
NOSTR_RELAYS=wss://relay.damus.io,wss://nostr-pub.wellorder.net

# OpenRouter Configuration
OPENROUTER_API_KEY=your_openrouter_api_key_here
LLM_MODEL_NAME=openai/gpt-4o-mini
LLM_BASE_URL=https://openrouter.ai/api/v1

# Agent Configuration
POSTING_INTERVAL_MINUTES=60
MIN_POSTING_INTERVAL=30
MAX_POSTING_INTERVAL=120

# Security Configuration (Optional but Recommended)
# Comma-separated list of authorized public keys (hex or bech32 format)
# If not set, all users can use commands (backward compatible, but less secure)
# AUTHORIZED_PUBKEYS=npub1...,npub1...
