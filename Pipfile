[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
agentstr-sdk = ">=0.6.15"
langchain-openai = ">=0.3.18"
python-dotenv = ">=1.1.0"
pydantic = ">=2.11.3"
pyyaml = ">=6.0"

[dev-packages]
pytest = ">=7.2.0"
pytest-asyncio = ">=1.0.0"

[requires]
python_version = "3.10"
